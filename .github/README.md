## Installation

```bash
my_vim_settings_install(){ curl -s -S -L https://raw.githubusercontent.com/gtsitsik/my-vim-settings/refs/heads/main/.dotfiles/install.sh | source <(cat) ; } ; my_vim_settings_install
